__pycache__/
*.pyc
*.egg-info/
.env
.streamlit/secrets.toml

# Local AlloyDB/Postgres connection secrets (see config/secrets.example.yaml).
secrets.yaml
**/secrets.yaml

# Tool caches (local only — regenerated on every run).
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtualenvs.
.venv/
venv/

# OS / editor cruft.
.DS_Store
*.swp

# Coverage artefacts from `pytest --cov` (CI runs it; local runs produce
# these files in whatever directory you run from).
.coverage
.coverage.*
htmlcov/
coverage.xml
